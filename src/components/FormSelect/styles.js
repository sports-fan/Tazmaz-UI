import { makeStyles } from "@mui/styles"

export default makeStyles(theme => ({
  main: {
    position: 'relative'
  },
  helperText: {
    position: 'absolute',
    width: 300,
    top: 57,
    left: '-190px'
  }
}))