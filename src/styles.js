import { makeStyles } from "@mui/styles"

export default makeStyles(theme => ({
  loginWithIcon: {
    width: 470,
    height: 50,
    border: '1, solid',
    borderColor: '#D1D6E7',
    borderRadius: 10
  }
}))